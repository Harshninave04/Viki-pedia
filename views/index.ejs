<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white ">
            <h1 class="flex justify-center items-center mb-10 text-[80px]">Viki-pedia</h1>
            <form action="/create" method="post">
                <input placeholder="Title goes here" type="text" name="title" class="w-full rounded-md bg-zinc-800 px-5 py-2 ">
                <textarea placeholder="Write your details here..." id="" name="details" class="w-full rounded-md block resize-none bg-zinc-800 px-5 py-2 mt-2"></textarea>
                <input type="submit" value="Create task" class="px-5 py-2 bg-white font-medium text-black my-4 rounded-lg">
            </form>
        </div>
        <div class="tasks p-10 flex gap-5 flex-wrap">
            <% if(files.length > 0){
                %>
                    <% files.forEach(function(val){
                        %>
                        <div class="task w-72 px-3 py-4 rounded-md bg-zinc-700">
                            <h1 class="text-3xl text-white tracking-tighter"><%= val %></h1>
                            <div class="flex justify-between mt-2">
                                <a href="/files/<%= val %>" class="inline-block text-blue-500">Read more</a>
                                <a href="/edit/<%= val %>" class="text-zinc-400">Edit filename</a>
                            </div>
                        </div>
                        <%
                    })                 
                    %>
                <%
            } else{
                %>
                    <h2 class="text-zinc-400 text-lg">No tasks yet.</h2>            
                <%
            }%>           
        </div>
    </div>

    <script src="/javascripts/script.js"></script>
</body>
</html>